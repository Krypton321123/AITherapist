import { Text } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context"

const mood = () => {
    return <SafeAreaView>
        <Text>Mood</Text>
    </SafeAreaView>
}

export default mood; 